<h1>All of our movies</h1>
<% @movies.each do |movie| %>
    <h3><%= link_to movie.title, movie_path(movie.id) %></h3>
    <p><%= movie.year%></p>
    <p><%= movie.genre %> - <%= movie.length %></p>
    <p>Reviews: <%= movie.count_reviews%></p>
    <p>Actors: <%= movie.actors.count%></p>
<% end %>